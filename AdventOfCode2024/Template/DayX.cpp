#include "../Utility/SupportFunc.h"
#include "DayX.h"


void DayX()
{
	myFile in = myFile();
	in.Open("DayX/in.txt");
	in.ReadFile();


	int sum = 0, sum2 = 0;

	for (int i = 0; i < in.nol; i++)
	{

	}

	cout << sum;
}